# GitHub-only release pipeline (yuurei distributes exclusively through
# GitHub Releases — no winget/Scoop/installers by choice).
#
# Push a tag like v0.1.0 to build a ReleaseFast portable zip:
#   bin/ghostty.exe (+ vendored conpty.dll/OpenConsole.exe, defterm proxy)
#   share/...        (shell integration, terminfo, themes)
#   LICENSE, README.md, THIRD_PARTY_NOTICES.md
#
# Binaries are unsigned; SmartScreen will warn on first run. SHA256
# checksums are published next to the zip.

on:
  push:
    tags: ["v*"]

name: Release

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - name: Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1

      # -Dcpu pins a portable ISA baseline. Without it Zig targets the CI
      # runner's native CPU, and windows-latest runners often have AVX-512;
      # the resulting binary dies with STATUS_ILLEGAL_INSTRUCTION on any
      # CPU without it (v0.2.10 was unrunnable on Zen 3). x86_64_v2 keeps
      # the zip runnable on ~15 years of x64; SIMD hot paths dispatch at
      # runtime so newer CPUs lose nothing.
      - name: Build (ReleaseFast)
        run: zig build -Doptimize=ReleaseFast -Dcpu=x86_64_v2

      # Runs on the same machine that built the binary, so it can NOT
      # catch ISA-baseline regressions like the above — only gross
      # packaging/startup breakage.
      - name: Smoke test
        run: zig-out\bin\ghostty.exe +version

      - name: Unit tests
        run: zig build -Dapp-runtime=none test

      - name: Assemble package
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          $pkg = "yuurei-$tag-windows-x64"
          New-Item -ItemType Directory "$pkg\bin" | Out-Null
          Copy-Item zig-out\bin\ghostty.exe "$pkg\bin\"
          Copy-Item vendor\conpty\conpty.dll, vendor\conpty\OpenConsole.exe "$pkg\bin\"
          # Default-terminal handoff COM proxy/stub, resolved beside the
          # exe by defterm.register(); omit it and registration points COM
          # at a missing DLL once the handoff gate flips.
          Copy-Item vendor\defterm\yuurei-defterm-proxy.dll "$pkg\bin\"
          Copy-Item -Recurse zig-out\share "$pkg\share"
          Copy-Item LICENSE "$pkg\"
          Copy-Item README.md "$pkg\"
          # Third-party notices: vendored conpty (MIT, microsoft/terminal)
          # plus libxev (MIT, mitchellh/libxev — statically linked). Since
          # the Zig 0.16 upgrade libxev is the upstream package dependency
          # (no longer a vendored fork); its LICENSE is read from the
          # build-populated zig-pkg dependency dir, globbed so version
          # bumps don't break this step.
          $libxevDir = Get-ChildItem zig-pkg -Directory -Filter 'libxev-*' | Select-Object -First 1
          $notices = (Get-Content vendor\conpty\NOTICE.md -Raw) + "`n`n---`n`n" +
            "## libxev (statically linked)`n`n" +
            "yuurei statically links libxev (mitchellh/libxev). License:`n`n" +
            (Get-Content (Join-Path $libxevDir.FullName 'LICENSE') -Raw)
          Set-Content -Path "$pkg\THIRD_PARTY_NOTICES.md" -Value $notices
          Compress-Archive -Path $pkg -DestinationPath "$pkg.zip"
          $hash = (Get-FileHash "$pkg.zip" -Algorithm SHA256).Hash.ToLower()
          Set-Content -Path "$pkg.zip.sha256" -Value "$hash  $pkg.zip"
          Get-Content "$pkg.zip.sha256"

      - name: Create GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "${{ github.ref_name }}"
          gh release create $tag `
            "yuurei-$tag-windows-x64.zip" `
            "yuurei-$tag-windows-x64.zip.sha256" `
            --title "yuurei $tag" `
            --generate-notes `
            --verify-tag
